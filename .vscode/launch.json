{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Tests",
            "type": "debugpy",
            "request": "launch",
            "module": "pytest",
            "args": [
                "-v",
                "--no-cov",
                "--pdb",
                //"${file}",
                //"-k", "test_verify_ca_drv_small",
                "tests"
            ],
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PATH": "${env:PATH}",
                "PYTHONPATH": "${env:PYTHONPATH}"
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}
