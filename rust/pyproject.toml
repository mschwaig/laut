[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "lautr"
version = "0.2.0"

[tool.maturin]
manifest-path = "lautr-py/Cargo.toml"
features = ["pyo3/extension-module"]
